# V5.7 GitHub Pages Compatibility Update

- Rebuilt asset links for GitHub Pages folder routing.
- Rebuilt JavaScript includes so shared header/footer load from the correct relative path.
- Added `.nojekyll` for direct static asset serving.
- Added `CNAME` with `techrecycle.co.za`.
- Removed Apache `.htaccess` and PHP contact handler because GitHub Pages is static hosting.
- Converted contact forms to a static form endpoint for `info@techrecycle.co.za`. First submission requires email confirmation.
- Added `/thank-you/` confirmation page.
- Kept all SEO recycling pages, blog, search, logo, sticky nav and AdSense pages from V5.6.
